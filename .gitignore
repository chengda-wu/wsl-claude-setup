# 敏感配置（含 API key / secret_key，勿提交）
settings.local.json
# settings.yml 从 searxng.example.yml 复制，含 secret_key
settings.yml
# docker-compose.override.yml 是本地代理配置，每台机器不同
docker-compose.override.yml
*.key
*.pem
.env

# Claude Code 产物
.playwright-mcp/
